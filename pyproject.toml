[project]
name = "pyuart-bridge"
version = "0.2.0"
description = "USB-serial ↔ TCP bridge for Termux on Android (esptool / mpremote compatible)"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyusb>=1.2",
]

[project.scripts]
pyuart-bridge = "main:main"
